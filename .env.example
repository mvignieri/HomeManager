# Environment
NODE_ENV=production

# Database
# Ottieni questo URL dal tuo provider database (Vercel Postgres, Supabase, Neon, etc.)
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# Firebase Client Configuration (Frontend)
# Ottieni questi valori dalla Firebase Console → Project Settings → General
VITE_FIREBASE_API_KEY=AIzaSy...
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abc123

# Firebase Cloud Messaging VAPID Key (Web Push Certificates)
# Firebase Console → Project Settings → Cloud Messaging → Web Push certificates
VITE_FIREBASE_VAPID_KEY=BM4x...

# Firebase Admin SDK (Backend)
# Firebase Console → Project Settings → Service Accounts → Generate New Private Key
# IMPORTANTE: Copia tutto il contenuto JSON su una singola riga
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n",...}

# Email Service Configuration
# Scegli uno dei provider seguenti:

# Opzione 1: SendGrid (Consigliato)
EMAIL_HOST=smtp.sendgrid.net
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=apikey
EMAIL_PASSWORD=SG.your-sendgrid-api-key
EMAIL_FROM="HomeTask" <noreply@yourdomain.com>

# Opzione 2: Mailgun
# EMAIL_HOST=smtp.mailgun.org
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=postmaster@yourdomain.com
# EMAIL_PASSWORD=your-mailgun-password
# EMAIL_FROM="HomeTask" <noreply@yourdomain.com>

# Opzione 3: Gmail (Solo per test, non per produzione)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password
# EMAIL_FROM="HomeTask" <your-email@gmail.com>

# Opzione 4: Resend (API moderna, non SMTP)
# Nota: Resend richiede integrazione custom, non usa SMTP standard
# RESEND_API_KEY=re_...
# EMAIL_FROM="HomeTask" <noreply@yourdomain.com>

# Development Only (non necessario in produzione)
# Queste variabili sono solo per sviluppo locale
# PORT=5000
